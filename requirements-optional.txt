# diy_model 可選依賴：缺了會自動降級，不裝也能跑核心流程
# 安裝：pip install -r requirements-optional.txt
# PDF 匯入
pypdf
# DOCX 匯入（已驗證 1.2.0）
python-docx==1.2.0
# 圖片 OCR（需另裝系統 tesseract）
Pillow==12.3.0
pytesseract
# 重排 CrossEncoder（已驗證 6.1.0）
sentence-transformers==6.1.0
# token 精算（缺了改字數估算）
tiktoken
# CLI 上下鍵歷史（缺了退化 input）
prompt_toolkit
# 測試與靜態檢查（版本單一來源在 requirements-dev.txt，勿在此另寫版本）
-r requirements-dev.txt
